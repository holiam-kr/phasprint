# docs/ is tracked on purpose. Plans, decisions and reviews are the record that lets a new
# session pick up where the last one stopped -- ignoring them defeats the reason they exist.
# docs/working/ holds transient notes; prune it by hand rather than by .gitignore.

# Windows
Thumbs.db
desktop.ini

# Editor / tool local settings
.claude/settings.local.json
.vscode/
.idea/
