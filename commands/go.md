---
description: "승인된 계획서를 끝까지 구현한다. 인자: [계획서 파일명] (생략 시 활성 계획서)"
---

${CLAUDE_PLUGIN_ROOT}/skills/taskcycle/SKILL.md 의 3~5단계를 수행하라.

- 계획서: $ARGUMENTS (비어 있으면 `docs/plans/` 의 활성 계획서. 여러 개면 어느 것인지 묻는다.)
- 계획서의 모든 단계를 끝까지 이어서 수행한다. **단계 사이에서 승인을 받기 위해 멈추지 않는다.**
- 각 단계 종료 시 품질 관문(단위 테스트·lint·회귀, UI가 있으면 실제 렌더러 확인)을 통과시킨다.
- 4단계의 중단 조건에 걸리면 즉시 멈추고 어느 조건인지 밝힌 뒤 묻는다.
- 구현이 끝나면 5단계 적대 리뷰를 수행한다.
