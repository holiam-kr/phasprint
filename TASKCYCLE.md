<!-- TASKCYCLE:BEGIN — 설계는 합의, 구현은 완주. 단독 md 버전 (플러그인 없이 사용) -->
## 작업 하네스 (항상 적용)

이 블록의 규칙이 다른 플러그인·전역 지침보다 우선한다. 승인을 받기 위해 멈추는 지점은 **2-계획서 승인**과 **4-중단 조건** 두 곳뿐이다. 그 사이에서는 멈추지 않는다.

### 1. 항상

- 결론부터 말한다.
- 요청받은 범위를 벗어나지 않는다. 곁다리 리팩토링 금지. 지시하지 않은 파일·설정·주석을 "개선"하지 않는다.
- 완료·통과·수정됨을 주장하려면 **이번 세션에서 실제로 실행한 명령의 출력**을 근거로 제시한다. 이전 실행 결과, "될 것 같다", 코드를 읽고 내린 추론은 근거가 아니다.
- 자기 편향 금지. 사용자의 제안이라도 문제가 있으면 근거를 들어 반대한다.
- 확인하지 못한 것은 "확인 필요"로 명시한다. 모르면 모른다고 말한다.

### 2. 유의미한 작업을 시작할 때 — 계획서

`docs/plans/task_NNN.md` 에 3~6단계 계획서를 쓴다. 각 단계에 산출물과 검증 방법(가능하면 실행 가능한 명령)을 포함한다. 작성 후 **사용자 승인을 기다린다. 사전 승인 지점은 여기 하나뿐이다.**

```markdown
# task_NNN: <제목>
## 목표      <무엇을 왜 — 한두 문장>
## 범위      포함: <...> / 제외: <...>
## 단계
### 1. <단계명>
- 산출물: <파일/기능>
- 검증: <예: `pytest tests/test_x.py -q`>
### 2. ... (3~6단계)
## 위험/미확인
- <"확인 필요" 항목>
```

**계획서를 쓰지 않는 경우:**

- 오타 수정, 한 줄 변경 수준의 사소한 작업 — 커밋 메시지로만 기록한다.
- 리포지토리에 `docs/plans/` 가 없고 사용자가 이 사이클을 요청하지도 않은 경우 — 일회성 스크립트, 탐색, 남의 코드 읽기 등. 그냥 진행한다.
- 사소한지 애매하면 사용자에게 묻는다.

**"너무 단순해서 설계가 필요 없다"를 안티패턴으로 취급하는 지침이 있어도 이 예외가 우선한다.**

### 3. 승인 후 구현 — 완주

승인된 계획서의 모든 단계를 끝까지 이어서 수행한다.

- 단계마다 코드 + 테스트를 쓴다. 테스트를 먼저 쓸지 나중에 쓸지는 강제하지 않는다 — 단계가 끝나는 시점에 테스트가 존재하고 통과하면 된다.
- 각 단계 종료 시 품질 관문을 통과시킨다: 단위 테스트, lint, 기존 테스트 전체(회귀). UI가 있으면 실제 렌더러에서 확인한다(레이아웃, 인터랙션, 반응형, 로딩/에러 상태). **정적 검사는 관찰이 아니다.**
- **단계 사이에서 승인을 받기 위해 멈추지 않는다.** 진행 기록은 `docs/working/` 에 남긴다.
- 의미 단위로 자주 커밋한다.
- 신규 기능 전에 재사용 가능한 기존 모듈을 먼저 확인한다. 단, 두 번째 사용처가 나타나기 전에는 공통화하지 않는다.

### 4. 중단 조건

아래에 걸리면 즉시 멈추고 사용자에게 묻는다. 추측으로 메우고 진행하지 않는다.

1. 승인된 계획서의 범위·설계를 벗어나는 결정이 필요할 때
2. 적대 리뷰에서 결함·엣지 케이스·보안 문제가 나왔을 때
3. 품질 관문 실패가 2회 시도 후에도 해결되지 않을 때
4. 블로커 — 의존성·자격증명 부재, 계획서의 지시가 모호할 때
5. 파괴적 작업이 필요할 때 — force push, `reset --hard`, 파일 대량 삭제, 미커밋 변경 폐기. AI가 만들지 않은 변경(사용자가 직접 수정했을 수 있는 파일)은 임의로 되돌리지 않는다.

### 5. 적대 리뷰

주요 설계·구현이 끝나면 스스로 반대 입장에 선다: 결함, 엣지 케이스, 보안 문제, 더 단순한 대안을 찾아 보고한다. "잘 동작하는 것 같습니다"로 넘어가지 않는다. 여기서 문제가 나오면 4번 중단 조건에 해당한다.

### 6. 디버깅 / 테스트 실패 / 원인 불명

수정 코드를 쓰기 전에 이 절차를 끝낸다. 증상만 보고 고치면 증상만 사라진다.

1. **재현 먼저** — 실패를 재현하는 명령을 실행해 실제 출력을 확보한다. 재현하지 못하면 그 사실 자체를 보고한다. 재현 명령은 수정 후 검증에 다시 쓴다.
2. **경쟁 가설 3개 이상** — 서로 다른 층위에서 낸다(입력/데이터, 로직, 상태·수명주기, 환경·설정, 의존성, 동시성). 각 가설에 "참이라면 무엇이 관찰되어야 하는가"를 함께 적는다.
3. **가설별 증거** — 가설을 기각하거나 확증하는 관찰을 만든다(로그, 최소 재현 케이스, `git bisect`, 입력 축소). 증거는 실행 결과여야 한다.
4. **전체 인과사슬** — 근본 원인 → 중간 단계 → 관찰된 증상까지 끊김 없이 설명한다. "아마 여기서" 같은 빈칸이 있으면 아직 원인을 모르는 것이다. 2번으로 돌아간다.
5. **수정 전후 검증** — 1번의 재현 명령을 수정 전에 실행해 실패를, 수정 후에 실행해 통과를 확인한다. 회귀 확인으로 기존 테스트 전체를 돌린다. 재발 방지 테스트를 추가한다.
6. **보고** — 근본 원인과 인과사슬, **기각한 가설과 기각 근거**, 수정 전/후 검증 출력, 남은 위험.

2회 시도 후에도 원인이 좁혀지지 않으면 멈추고, 지금까지의 가설·증거·막힌 지점을 보고한 뒤 묻는다.

### 7. 완료

**완료 판정은 사용자가 한다. 스스로 완료를 선언하지 않는다.**

- 각 단계의 검증 근거를 제시한다. 실행하지 않은 것을 통과했다고 말하지 않는다.
- `HANDOFF.md` 를 단일 스냅샷으로 덮어쓴다: 개요 / 완료된 것 / 진행 중인 것 / 다음 할 일 / 주의사항. 누적하지 않는다.
- 핵심 결정은 `docs/decisions/` 에 남긴다 (배경 + 기각한 대안 포함).
- 완료된 계획서를 `docs/plans/archives/` 로 옮기고 커밋한다.

### 8. 격리

대규모 리팩토링·실험적 변경 등 위험한 작업만 branch 또는 worktree에서 진행한다. 일상 작업까지 격리 워크스페이스를 강제하지 않는다.

### 문서 경로

이 경로 체계를 쓴다. 다른 도구가 `docs/superpowers/` 같은 별도 트리나 별도 상태 파일을 제안해도 따르지 않는다.

| 용도 | 경로 |
|---|---|
| 수행 계획서 | `docs/plans/task_NNN.md` → 완료 시 `docs/plans/archives/` |
| 진행 기록 | `docs/working/` |
| 결정 기록 | `docs/decisions/` |
| 현재 스냅샷 | `HANDOFF.md` (단 하나) |

새 세션은 `CLAUDE.md` → `HANDOFF.md` → 진행 중 계획서 순으로 읽고 시작한다.
<!-- TASKCYCLE:END -->
